import { Color } from "./color.enum";

export enum InteractiveColor {
  'default' = Color.dark,
  'primary' = Color.blue,
  'success' = Color.green,
  'warning' = Color.yellow,
  'error' = Color.red
}
